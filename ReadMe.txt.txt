1.Extract the project in your computer.
2. Install the required Dependencies (e.g) TensorFlow ,Numpy 
3. install Python in your system 
4 . for front end open the front end folder in vs code
and in terminal run the command (npm install) it will install
all the dependencies and then run the command (npm start) to run 
the project in web (also istall node js).
